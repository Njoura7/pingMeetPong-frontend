export interface Match {
    _id: string;
    name: string;
    code: string;
    place: string;
    date: Date;
  }

  export interface User {
    _id: string;
    username: string;
    avatar: string;
  }